server {
    listen 80;

    location / {
        # 將收到的請求轉發給名為 'app' 的容器的 8000 port
        proxy_pass http://app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}